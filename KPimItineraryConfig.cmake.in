@PACKAGE_INIT@

include(CMakeFindDependencyMacro)
find_dependency(Qt5Gui)
find_dependency(KF5Mime)
if (@HAVE_KCAL@)
    find_dependency(KF5CalendarCore)
endif()
find_dependency(KF5Contacts)
find_dependency(KPimPkPass)

if (NOT @BUILD_SHARED_LIBS@)
    find_dependency(ZLIB)
    if (@HAVE_POPPLER@)
        find_dependency(Poppler)
    endif()
    if (@HAVE_ZXING@)
        find_dependency(zxing)
    endif()
    if (@HAVE_LIBXML2@)
        find_dependency(LibXml2)
    endif()
endif()

include("${CMAKE_CURRENT_LIST_DIR}/KPimItineraryTargets.cmake")
@PACKAGE_INCLUDE_QCHTARGETS@
